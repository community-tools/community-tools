<h2>New Organization</h2>

<%= render "form.html", changeset: @changeset,
                        action: organization_path(@conn, :create) %>

<span><%= link "Back", to: organization_path(@conn, :index) %></span>
